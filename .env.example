# Example environment variables

# App port
PORT=3000

# MongoDB connection string for local development
MONGO_URI=mongodb://127.0.0.1:27017/baokim_dev

# MongoDB connection string for Docker (used by docker-compose)
MONGO_URL=mongodb://mongo:27017/baokim_dev

# Secret for signing session cookies (set this in your local .env, never commit it)
SESSION_SECRET=change_this_to_a_strong_secret
